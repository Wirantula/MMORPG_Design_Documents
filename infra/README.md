# Infrastructure
## Files
- `compose/docker-compose.yml` - local core stack
- `compose/docker-compose.observability.yml` - prometheus + grafana starter stack
- `compose/prometheus.yml` - scrape config
- `env/.env.example` - infrastructure env template
- `migrations/` - migration placeholder and SQL history
